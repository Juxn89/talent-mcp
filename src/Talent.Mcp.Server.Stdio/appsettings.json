{
  "_comment": "No connection string and no signing key here on purpose: production supplies both from the environment, and the host refuses to start without them. Dev values live in appsettings.Development.json.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Talent": {
    "DefaultPageSize": 20,
    "MaxPageSize": 100
  }
}
