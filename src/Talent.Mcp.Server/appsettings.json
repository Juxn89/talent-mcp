{
  "_comment": "No connection string, signing key or OAuth issuer here on purpose: production supplies all three from the environment, and the host refuses to start without them. Dev values live in appsettings.Development.json.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Talent": {
    "DefaultPageSize": 20,
    "MaxPageSize": 100
  }
}
